import Image from "next/image";

const HeroImage = () => {
  return <Image src="/hero.png" width={1200} height={630} alt="This image shows the installation steps of hoocs library" />;
};

export default HeroImage;
